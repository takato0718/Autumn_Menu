<div class="max-w-5xl mx-auto fade-in-up">
  <!-- パンくずナビ -->
  <nav class="mb-8">
    <ol class="flex items-center space-x-3 text-sm">
      <li>
        <%= link_to root_path, class: "flex items-center text-orange-600 hover:text-orange-700 font-medium transition-colors duration-300" do %>
          <span class="mr-1">🏠</span>
          ホーム
        <% end %>
      </li>
      <li><span class="mx-2 text-gray-400">›</span></li>
      <li class="text-gray-700 font-medium"><%= @recipe.name %></li>
    </ol>
  </nav>

  <div class="autumn-card overflow-hidden">
    <!-- レシピ画像 -->
    <div class="relative">
    <% if @recipe.image_url.present? %>
        <img src="<%= @recipe.image_url %>" alt="<%= @recipe.name %>" 
             class="w-full h-72 md:h-96 object-cover">
    <% else %>
        <div class="w-full h-72 md:h-96 autumn-gradient flex items-center justify-center">
          <span class="text-white text-8xl drop-shadow-2xl">🍂</span>
      </div>
    <% end %>
      
      <!-- 難易度バッジ -->
      <div class="absolute top-6 right-6">
        <span class="difficulty-badge text-lg px-4 py-2 <%= 
          case @recipe.difficulty
          when '簡単' then 'difficulty-easy'
          when '普通' then 'difficulty-normal'  
          when '難しい' then 'difficulty-hard'
          else 'difficulty-easy'
          end
        %>">
          <%= @recipe.difficulty %>
        </span>
      </div>
    </div>

    <div class="p-8 md:p-12">
      <!-- レシピタイトルと基本情報 -->
      <div class="mb-10">
        <h1 class="text-4xl md:text-5xl font-bold text-gray-800 mb-6 leading-tight">
          <%= @recipe.name %>
        </h1>
        
        <% if @recipe.description.present? %>
          <p class="text-lg text-gray-600 mb-6 leading-relaxed max-w-3xl">
            <%= @recipe.description %>
          </p>
        <% end %>
        
        <!-- 統計情報カード -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
          <div class="bg-gradient-to-br from-orange-50 to-amber-50 p-6 rounded-xl border border-orange-100">
            <div class="flex items-center text-orange-600 mb-2">
              <svg class="w-6 h-6 mr-3" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd"></path>
              </svg>
              <span class="font-semibold">調理時間</span>
            </div>
            <div class="text-2xl font-bold text-gray-800">
              <%= @recipe.cooking_time.present? ? "#{@recipe.cooking_time}分" : "未設定" %>
            </div>
          </div>
          
          <div class="bg-gradient-to-br from-green-50 to-emerald-50 p-6 rounded-xl border border-green-100">
            <div class="flex items-center text-green-600 mb-2">
              <span class="text-xl mr-3">🥄</span>
              <span class="font-semibold">食材数</span>
            </div>
            <div class="text-2xl font-bold text-gray-800">
              <%= @recipe.ingredient_list.length %>種類
            </div>
          </div>
          
          <div class="bg-gradient-to-br from-blue-50 to-indigo-50 p-6 rounded-xl border border-blue-100">
            <div class="flex items-center text-blue-600 mb-2">
              <span class="text-xl mr-3">⭐</span>
              <span class="font-semibold">難易度</span>
            </div>
            <div class="text-2xl font-bold text-gray-800">
              <%= @recipe.difficulty %>
            </div>
          </div>
        </div>
      </div>

      <!-- メインコンテンツ -->
<div class="container mx-auto max-w-6xl px-4">
  <div class="grid lg:grid-cols-5 gap-12">
    <!-- 材料セクション -->
    <div class="lg:col-span-2">
      <div class="sticky top-8">
        <h2 class="text-3xl font-bold text-gray-800 mb-6 flex items-center">
          <span class="text-orange-600 mr-3 text-4xl">🥕</span>
          材料
        </h2>
        <div class="bg-gradient-to-br from-orange-50 to-amber-50 p-6 rounded-xl border border-orange-100">
          <ul class="space-y-4">
            <% @recipe.ingredient_list.each_with_index do |ingredient, index| %>
              <li class="flex items-start group">
                <span class="flex-shrink-0 w-6 h-6 bg-orange-500 text-white rounded-full flex items-center justify-center text-sm font-bold mr-4 mt-0.5 group-hover:bg-orange-600 transition-colors duration-300">
                  <%= index + 1 %>
                </span>
                <span class="text-gray-700 font-medium leading-relaxed">
                  <%= ingredient.strip %>
                </span>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>

        <!-- 作り方セクション -->
    <div class="lg:col-span-3">
      <h2 class="text-3xl font-bold text-gray-800 mb-6 flex items-center">
        <span class="text-orange-600 mr-3 text-4xl">👩‍🍳</span>
        作り方
      </h2>
      <div class="bg-gradient-to-br from-gray-50 to-slate-50 p-8 rounded-xl border border-gray-100">
        <div class="prose prose-lg max-w-none">
          <% @recipe.instructions.split("\n").each_with_index do |step, index| %>
            <% next if step.strip.empty? %>
            <div class="mb-6 p-6 bg-white rounded-lg shadow-sm border border-gray-100 hover:shadow-md transition-shadow duration-300">
              <div class="flex items-start">
                <span class="flex-shrink-0 w-8 h-8 bg-gradient-to-r from-orange-500 to-red-500 text-white rounded-full flex items-center justify-center text-sm font-bold mr-4 mt-1">
                  <%= index + 1 %>
                </span>
                <div class="flex-1">
                  <p class="text-gray-700 leading-relaxed text-lg">
                    <%= step.strip %>
                  </p>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>

      <!-- アクションボタン -->
      <div class="mt-12 pt-8 border-t border-gray-200">
    <div class="flex flex-col sm:flex-row gap-6 justify-center">
      <%= link_to root_path, 
          class: "flex-1 max-w-xs mx-auto text-center bg-gradient-to-r from-orange-500 to-red-500 hover:from-orange-600 hover:to-red-600 text-white font-semibold py-3 px-6 rounded-lg shadow-lg hover:shadow-xl transition-all duration-300 transform hover:scale-105" do %>
        <span class="flex items-center justify-center">
          <span class="mr-2">🏠</span>
          レシピ一覧に戻る
        </span>
      <% end %>
      
      <button onclick="window.scrollTo({top: 0, behavior: 'smooth'})" 
              class="flex-1 max-w-xs mx-auto autumn-button">
        <span class="flex items-center justify-center">
          <span class="mr-2">⬆️</span>
          ページトップへ
        </span>
      </button>
    </div>
  </div>

  <!-- Xシェアボタン（中央揃え） -->
  <div class="mt-8 flex justify-center">
    <%= link_to twitter_share_with_image_url(@recipe), 
                target: "_blank", 
                rel: "noopener noreferrer",
                class: "inline-flex items-center gap-2 bg-gradient-to-r from-gray-800 to-black hover:from-black hover:to-gray-900 text-white font-semibold py-3 px-6 rounded-lg shadow-lg hover:shadow-xl transition-all duration-300 transform hover:scale-105" do %>
      <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
        <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/>
      </svg>
      Xでシェア
    <% end %>
  </div>
</div>
  
  <!-- 関連レシピ提案 -->
  <div class="mt-16 fade-in-up">
    <div class="text-center mb-8">
      <h3 class="text-2xl font-bold text-gray-800 mb-4 flex items-center justify-center">
        <span class="mr-3 text-3xl">💡</span>
        他のおすすめレシピ
      </h3>
      <p class="text-gray-600">同じ食材を使った他のレシピもお試しください</p>
    </div>
    
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <% Recipe.where.not(id: @recipe.id).limit(3).each do |related_recipe| %>
        <div class="autumn-card overflow-hidden group">
          <div class="relative">
            <% if related_recipe.image_url.present? %>
              <img src="<%= related_recipe.image_url %>" alt="<%= related_recipe.name %>" 
                   class="w-full h-40 object-cover group-hover:scale-110 transition-transform duration-500">
            <% else %>
              <div class="w-full h-40 autumn-gradient flex items-center justify-center group-hover:scale-110 transition-transform duration-500">
                <span class="text-white text-4xl">🍂</span>
              </div>
            <% end %>
          </div>
          
          <div class="p-6">
            <h4 class="text-lg font-bold text-gray-800 mb-2 group-hover:text-orange-600 transition-colors duration-300">
              <%= related_recipe.name %>
            </h4>
            
            <div class="flex items-center justify-between text-sm text-gray-500 mb-4">
              <span class="flex items-center">
                <svg class="w-4 h-4 mr-1 text-orange-500" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd"></path>
                </svg>
                <%= related_recipe.cooking_time.present? ? "#{related_recipe.cooking_time}分" : "未設定" %>
              </span>
              <span class="difficulty-badge <%= 
                case related_recipe.difficulty
                when '簡単' then 'difficulty-easy'
                when '普通' then 'difficulty-normal'  
                when '難しい' then 'difficulty-hard'
                else 'difficulty-easy'
                end
              %>">
                <%= related_recipe.difficulty %>
              </span>
            </div>
            
            <%= link_to recipe_path(related_recipe), 
                class: "block w-full text-center bg-gradient-to-r from-orange-500 to-red-500 hover:from-orange-600 hover:to-red-600 text-white font-semibold py-2 px-4 rounded-lg transition-all duration-300 transform hover:scale-105" do %>
              レシピを見る
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>

</style>
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd"></path>
            </svg>
            <span>調理時間: <%= @recipe.cooking_time.present? ? "#{@recipe.cooking_time}分" : "未設定" %></span>
          </div>
          <span class="bg-orange-100 text-orange-800 px-3 py-1 rounded-full">
            <%= @recipe.difficulty %>
          </span>
        </div>
      </div>